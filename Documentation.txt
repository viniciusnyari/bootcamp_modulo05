#Documentação do repositório

#Criar uma aplicação React com webpack e babel tudo configurado - criar em um diretório antes para evitar crias a pasta - com template TypeScript
yarn create react-app bootcamp_modulo05 --template typescript

#Criar uma aplicação React com webpack e babel tudo configurado - criar em um diretório antes para evitar crias a pasta
yarn create react-app bootcamp_modulo05

#Rodando a aplicação - essa configuração está no 'package.json'
yarn react-scripts start

#Excluir os comentários e o arquivo 
manifest.json

#Excluir esses e zelar para evitar os erros -  remover as referências
App.css
App.test.js
index.css
logo.svg
serviceWorker.js

#Gerando o arquivo '.editorConfig'
Botão direito nos arquivos em uma área livre e escolher 'Generator .editorConfig'
Será gerado o arquivo

#Adicionar o EsLint como dependência de desenvolvimento
yarn add eslint -D

#Inicializando o 'eslint'
yarn eslint --init
Responda as perguntas conforme abaixo: 
? How would you like to use ESLint? To check syntax, find problems, and enforce code style
? What type of modules does your project use? JavaScript modules (import/export)
? Which framework does your project use? React
? Does your project use TypeScript? No
? Where does your code run? Browser
? How would you like to define a style for your project? Use a popular style guide
? Which style guide do you want to follow? Airbnb: https://github.com/airbnb/javascript
? What format do you want your config file to be in? JavaScript
Checking peerDependencies of eslint-config-airbnb@latest
Responsa 'Y' para tudo o que for questionado e aguarde a instalação.

#Instalando pretier - deixa o código mais bonito - eslint procura erros (ausência de pontos)
yarn add prettier eslint-config-prettier eslint-plugin-prettier babel-eslint -D

#Instalar para fazer roteamento SPA
yarn add react-router-dom

#instalando snipped para criar automaticamente códigos
Instale a extensão da 'Rocketseat ReactJS' e 'Rocketseat React Native'
Aí basta digitar 'rfc' para criar um novo componente do tipo função

#Instalando componentes de estilo
yarn add styled-components

#Instalando extensão do Styled-compoenent para não dar erros
vscode-styled-components

#Algumas informações a cerca de css
Por padrão o html ocupa sempre o espaço necessário, mas estamos alterando para que ocupe toda a altura da tela
min-height: 100%;

Para que a fonte fique melhor definida
-webkit-font-smoothing: antialiased !important;
